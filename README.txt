1. Install your local environment outside the 165 folder (i.e. make your env folder a sibling of 165 folder)
2. Below are the commands to run
   (For windows)
   - python -m venv env
   - easy_install pip
   - pip install django==1.8.4
   - pip install django-cors-headers